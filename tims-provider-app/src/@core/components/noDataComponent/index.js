import React from "react";
import { FormattedMessage } from "react-intl"

function NoData() {
  return <div style={{padding: "15px 0px", display: 'flex', alignItems: "center", color: "#263238"}}>Il n'y a aucune donnée</div>
}

export default NoData;
